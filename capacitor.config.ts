import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.myliege.playgroundapp',
  appName: 'playgroundApp',
  webDir: 'dist/playground-app',
};

export default config;
